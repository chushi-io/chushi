import * as React from "react";
import Sidebar from "../components/Sidebar";

const Root = () => {
    return (
        <div style={{display: "flex", height: "100vh"}}>
            <Sidebar>

            </Sidebar>
        </div>
    )
}

export default Root;
