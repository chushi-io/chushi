
<nav class="mb-3" aria-label="breadcrumb">
  <ol class="breadcrumb mb-0">
    <li class="breadcrumb-item"><a href="<%= run_tasks_path(@organization.name) %>">Run Tasks</a></li>
    <li class="breadcrumb-item active">New</li>
  </ol>
</nav>

<h2 class="mb-4">Create Run Task</h2>

<div class="row">
  <div class="col-xl-9">
    <%= form_with model: @run_task, class: "row g-3 mb-6" do |form| %>
      <div class="col-sm-6 col-md-8">
        <div class="form-floating">
          <%= form.text_field :name, class: "form-control" %>
          <label for="floatingInputGrid">Name</label>
        </div>
      </div>
      <div class="col-sm-6 col-md-8">
        <div class="form-floating">
          <%= form.text_field :description, class: "form-control" %>
          <label for="floatingInputGrid">Description</label>
        </div>
      </div>
      <div class="col-sm-6 col-md-8">
        <div class="form-floating">
          <%= form.text_field :url, class: "form-control" %>
          <label for="floatingInputGrid">URL</label>
        </div>
      </div>

      <div class="col-sm-6 col-md-8">
        <div class="form-floating">
          <%= form.password_field :hmac_key, class: "form-control" %>
          <label for="floatingInputGrid">HMAC Key</label>
        </div>
      </div>

      <div class="col-sm-6 col-md-8">
        <div class="form-check mb-0">
          <%= form.check_box :enabled, class: "form-check-input" %>
          <%= form.label :enabled, class: "form-check-label mb-0" %>
        </div>
      </div>
      <div class="col-12 gy-6">
        <div class="row g-3 justify-content-end">
          <div class="col-auto">
            <%= link_to "Cancel", run_tasks_path(@organization.name), class: "btn btn-phoenix-primary px-5" %>
          </div>
          <div class="col-auto">
            <%= form.submit "Create Run Task", class: "btn btn-primary px-5 px-sm-15" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
