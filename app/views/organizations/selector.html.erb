<%= form_with do |form| %>
  <%= form.select :organization_id, @organizations.map{ |org| [org.name, org.id]} %>
  <%= form.submit "Change Organization" %>
<% end %>

<a href="<%= organizations_new_path %>">Create Organization</a>